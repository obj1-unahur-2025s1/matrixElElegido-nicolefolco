import matrix.*

describe "la matrix" {

  test "todos saltan de la emocion" {
    nave.naveAcelera()
    assert.equals(false, morfeo.estaDescansado())
  }
  
  test "la nave choca"{
    nave.naveChoca()
    assert.equals(50, neo.energia())
  }

  test "la nave choca y no quedan pasajeros"{
    nave.naveChoca()
    assert.equals({}, nave.pasajeros())
  }

  test "neo es el elegido"{
    assert.equals(true, neo.esElElegido())
  }
}